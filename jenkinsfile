pipeline {
    agent any

    stages {
        stage('Say Hello') {
            steps {
                echo 'Hello, World!'
            }
        }

        stage('Create File') {
            steps {
                sh '''
                    echo "This is a test file in basicpyhoncode repo." > test.txt
                    cat test.txt
                '''
            }
        }
    }
}
